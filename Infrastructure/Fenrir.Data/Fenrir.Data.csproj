<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <PackageReference Include="CaeriusNet"/>
    <PackageReference Include="Konscious.Security.Cryptography.Argon2"/>
    <!-- Only for catching SqlException.Number on the AccountSessionRepository claim-conflict retry loop;
         CaeriusNet already pulls this in transitively, this makes the direct dependency explicit. -->
    <PackageReference Include="Microsoft.Data.SqlClient"/>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Fenrir.Data.Abstractions\Fenrir.Data.Abstractions.csproj"/>
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
