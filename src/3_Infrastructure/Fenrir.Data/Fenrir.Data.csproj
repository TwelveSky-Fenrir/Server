<Project Sdk="Microsoft.NET.Sdk">

  <!-- Unique porte SQL via CaeriusNet (procédures stockées only), DTO par schéma, TVP, write-behind.
       ISOLÉE : ne référence NI Core, NI Network, NI aucun Application (voir doc 02 §5.3). Ne dépend que
       d'Observability (log structuré des appels). Fusionne (Lot F6) l'ancien Data.Abstractions en dossier
       Abstractions/. ApplicationFirewall part vers Security (F2/F6), WorldData passe derrière interface. -->

  <ItemGroup>
    <PackageReference Include="CaeriusNet"/>
    <PackageReference Include="Konscious.Security.Cryptography.Argon2"/>
    <!-- Direct pour capter SqlException.Number (retry de conflit In-Memory OLTP) ; CaeriusNet le tire déjà. -->
    <PackageReference Include="Microsoft.Data.SqlClient"/>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Fenrir.Observability\Fenrir.Observability.csproj"/>
  </ItemGroup>

</Project>
