{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "Game": {
    // The effective default lives HERE and not in GameServerOptions: the configuration binder CONCATENATES
    // bound array items onto a non-empty compiled-in default (see GameServerOptions.Maps remarks). AppHost
    // overrides these entries key-by-key (Game__Maps__0=...) per shard.
    "Maps": [
      1
    ]
  }
}
