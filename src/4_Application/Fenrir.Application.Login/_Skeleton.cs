namespace Fenrir.Application.Login;

/// <summary>
/// Marqueur du squelette <c>Fenrir.Application.Login</c> (Lot F1 — Fondations). Slice serveur Login :
/// paquets Login, <c>LoginFrameDispatcher</c>, handlers et services (auth, redirection par ticket).
/// Contenu migré au Lot F6 depuis <c>Serialization.Login</c>, <c>Dispatch.Login</c>, et les projets
/// <c>Application.Login.*</c> (le <c>RateLimiting</c> part vers <c>Security</c>, correctif F5).
/// </summary>
public static class AssemblyInfo
{
    /// <summary>Nom logique de l'assembly (repère de squelette).</summary>
    public const string Name = "Fenrir.Application.Login";
}
